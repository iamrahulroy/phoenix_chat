<h2>Welcome to ChatApp</h2>

<%= if @user do %>
  <div class="well">
    <div id="messages"></div>
    <input id="username" type="text" value="<%= @user.username %>" readonly></input>
    <input id="chat-input" type="text"></input>
  </div>
<% else %>
  <p>You need to <%= link "Log in", to: session_path(@conn, :new) %> to use ChatApp. Chat is only enabled for logged in users. Thank you!</p>
<% end %>
